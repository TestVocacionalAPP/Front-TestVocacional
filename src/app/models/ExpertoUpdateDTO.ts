export interface ExpertoUpdateDTO {
  nombre: string;
  apellido: string;
  correo: string;  // Asegúrate de que esta propiedad esté incluida
  telefono: string;  // Asegúrate de que esta propiedad esté incluida
  especialidad: string;
  descripcion: string;
  tarifa: number;
}
